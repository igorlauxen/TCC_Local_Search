import json

with open ("user1.json") as json_file:
    json_data = json.load(json_file)
    print(json_)

class Transaction:
    def __init__(self, name, idtransaction, order):
        self.field = field
        self.qtdUsage = qtdusage
        self.order = order

    def screen(self, name, idscreen):
        self.name = name
        self.idScreen = idscreen
        
